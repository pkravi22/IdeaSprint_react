import React from "react";
import AuthPage from "../component/authpage/AuthPage";

const SignUp = ({ tab }) => {
  return (
    <div>
      <AuthPage tab={tab} />
    </div>
  );
};

export default SignUp;
